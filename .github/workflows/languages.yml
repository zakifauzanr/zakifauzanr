name: Generate Languages

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  languages:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Generate languages SVG
      # kemudian commit hasilnya ke repository
